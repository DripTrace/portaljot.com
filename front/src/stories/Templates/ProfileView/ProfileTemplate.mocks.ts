import { IProfileTemplate } from "./ProfileTemplate";

const profileView: IProfileTemplate = {
    profileTextProp: "This is the profile view",
};

export const mockProfileTemplateProps = {
    profileView,
};
