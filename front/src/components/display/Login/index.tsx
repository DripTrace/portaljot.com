import { LoginContainer } from "./LoginElements";

const Login = () => {
    return (
        <>
            <LoginContainer>Sign in with GitHub</LoginContainer>
        </>
    );
};

export default Login;
