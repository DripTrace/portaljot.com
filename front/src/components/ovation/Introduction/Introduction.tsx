import { IntroStart, IntroEnd } from "@/components";

const Introduction = () => {
    return (
        <header className="introduction-container">
            <IntroStart />
            <IntroEnd />
        </header>
    );
};

export default Introduction;
