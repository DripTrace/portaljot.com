"use client";

import { Banner, About } from "@/components";

const Content = () => {
    return (
        <main className="content">
            <Banner />
            <About />
        </main>
    );
};

export default Content;
