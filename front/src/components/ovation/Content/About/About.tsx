import { User, Gallery } from "@/components";

const About = () => {
    return (
        <section className="about">
            <User />
            <Gallery />
        </section>
    );
};

export default About;
